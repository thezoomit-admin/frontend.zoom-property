/** Same surface as the review cards — used on every landing card. */
export const landingCardClass =
  "rounded-lg border border-border bg-card shadow-xs transition-shadow duration-300 hover:shadow-md";

/** Extra-bold titles that wrap on every width. */
export const landingTitleClass = "!font-extrabold tracking-tight lg:whitespace-normal";

